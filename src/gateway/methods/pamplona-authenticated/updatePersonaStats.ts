// originally encrypted

import { ServerParams } from '../..'

export default {
  name: 'PamplonaAuthenticated.updatePersonaStats',
  execute: (params: {}, server: ServerParams) => {
    console.log(params, server)
  },
}
